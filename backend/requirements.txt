# Requires Python >=3.12
# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.49.0
starlette==1.3.1
python-multipart==0.0.32

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.0.0
pydantic==2.13.4           # 2.11+ required; verified compatible with langchain-core 1.4.7
pydantic-settings==2.14.1
pydantic_core==2.46.4

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.13.1

# ── Auth ──────────────────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt==3.2.2              # DO NOT upgrade to 4.x — breaks passlib
python-jose[cryptography]==3.3.0
cryptography==49.0.0
email-validator==2.1.0

# ── Task queue ────────────────────────────────────────────────────────────────
celery==5.3.4
redis==5.0.1
kombu==5.6.2
billiard==4.2.4
amqp==5.3.1
vine==5.1.0

# ── LangChain / LLM ───────────────────────────────────────────────────────────
langchain==1.3.9
langchain-core==1.4.7
langchain-community==0.4.2
langchain-openai==1.3.2
langchain-aws==1.5.1
langchain-text-splitters==1.1.2
langchain-classic==1.0.8
langsmith==0.8.15
openai==2.41.1
tiktoken==0.13.0

# ── AWS ───────────────────────────────────────────────────────────────────────
boto3==1.43.29
botocore==1.43.29
s3transfer==0.18.0

# ── Document parsing ──────────────────────────────────────────────────────────
docling==2.103.0
docling-core==2.82.0
docling-ibm-models==3.13.3
docling-parse==6.2.0
docling-slim==2.103.0
python-docx==1.2.0         # required for Word export (docx_export.py)
docxtpl>=0.16.0            # Jinja2 templating inside .docx files

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu==1.14.3
numpy==2.4.6

# ── ML / vision (docling dependencies) ───────────────────────────────────────
# CPU-only torch — avoids 2+ GB of CUDA libs on CPU-only servers (AWS instances without GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
torchvision==0.27.0+cpu
transformers==5.12.0
safetensors==0.8.0
tokenizers==0.22.2
huggingface_hub==1.19.0
accelerate==1.14.0
opencv-python==4.13.0.92
pillow==12.2.0

# ── HTTP / networking ─────────────────────────────────────────────────────────
httpx==0.28.1              # 0.28+ required for docling-slim 2.103
httpx-sse==0.4.3
requests==2.34.2
aiohttp==3.14.1

# ── Utilities ─────────────────────────────────────────────────────────────────
anyio==3.7.1
orjson==3.11.9
python-dateutil==2.9.0.post0
tenacity==8.5.0
typing_extensions==4.15.0
PyYAML==6.0.3
rich==15.0.0
packaging==23.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.0.0
pytest-asyncio==0.23.5
